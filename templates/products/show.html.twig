{% extends 'base.html.twig' %}

{% block title %}Products{% endblock %}

{% block body %}
    <h1>Products</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom du produit</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>
                    Catégorie
                </th>
                <td>
                    {{ product.category.name }}
                </td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex">
        <a href="{{ path('products_index') }}" class="btn btn-primary ml-1"><i class="fa fa-arrow-left"></i></a>

        <a href="{{ path('products_edit', {'id': product.id}) }}" class="btn btn-warning ml-1"><i class="fa fa-edit text-light"></i></a>

        {{ include('products/_delete_form.html.twig') }}
    </div>
{% endblock %}
