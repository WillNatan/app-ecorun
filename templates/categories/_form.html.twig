{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success ml-1 mt-3">{{ button_label|default('Enregistrer') }}</button>
<a href="{{ path('categories_index') }}" class="btn btn-primary ml-1 mt-3"><i class="fa fa-arrow-left"></i></a>
{{ form_end(form) }}
