{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">{{ button_label|default('Enregistrer') }}</button>
<a href="{{ path('user_index') }}" class="btn btn-primary mt-3"><i class="fa fa-arrow-left"></i></a>
{{ form_end(form) }}
